DATABASE_URL=postgres://dev:dev@localhost:5433/dcp
OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-5.6-terra   # default if unset

# Leave unset for local dev (falls back to http://127.0.0.1:30001 and reads page images off
# disk). In production, set to the deployed Fly.io URL - see deploy/pixelrag/README.md.
# PIXELRAG_URL=https://dcp-hornsby-search.fly.dev
